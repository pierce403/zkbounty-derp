import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#4dfcff', // Example: Neon blue
    },
    // ... other theme properties
  },
  // ... other customizations
});

export default theme;
